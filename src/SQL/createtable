/* creating table */
CREATE TABLE portTable (
  query_id serial PRIMARY KEY, first_name VARCHAR(50) NOT NULL, last_name VARCHAR(50), email VARCHAR(50) NOT NULL, description VARCHAR(500), created_on TIMESTAMP NOT NULL
);